import Image from 'next/image'

--- 

# Step 1

Setup `Vite + React + TypeScript`.

* 🔥 [`vite`](https://github.com/vitejs/vite) hot reloading and page refreshing in blessing fast.
* 👀 [`react`](https://github.com/facebook/react) one library covers two worlds— web and native.
* 👮 [`typescript`](https://www.typescriptlang.org/) catch types errors early in editor.

It is so easy to install React + Typescript using Vite. Open terminal and type:

~~~terminal
yarn create vite
~~~

<video src="/work/react-locally/video1.mov" autoPlay loop />

Now go to project dir and run: 

~~~terminal
yarn install
~~~